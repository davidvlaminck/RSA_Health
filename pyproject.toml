[project]
name = "rsa-health"
version = "0.1.0"
description = "Health monitor for cloud server"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn>=0.23.0",
    "python-arango>=7.0.0",
    "psycopg2-binary>=2.9.0",
    "tomli>=2.0.0",
    "psutil>=5.9.0",
]
